@model IEnumerable<Upload>

@foreach (var upload in Model){
    @upload.UploadTitle<br />
    <audio controls>
        <source src="/Songs/@upload.UploadPath" type="audio/mpeg">
        Your browser does not support the audio element.
    </audio><br />
}